# TODO

- Phase 1 Toggl-inspired features now implemented: favorites, task tracking, project estimate/budget dashboard, saved reports, and task-aware reporting filters.
- Next phase implemented: project row overflow menus, optional TOTP, weekly calendar, account settings, workspace switcher, project membership editing, and historical/user cost rates.
- Dropdown/favicons follow-up complete for current action density; add more row overflow menus only when rows gain multiple secondary actions.
- Add PostgreSQL repository implementation when needed.
- Add richer invoice templates after validating the lightweight HTML/CSV path.
- Add data import tests against representative Kimai exports.
- Sidebar collapse explicitly deferred: current workflow groups are still small enough that visible navigation is clearer.
- Add saved report edit/delete/share UI after report usage patterns are clearer.
- Add profitability reporting after historical rate and user cost data has been validated in production.
- Add scheduled/shared reports with signed expiring tokens.
- Add approvals and locked periods only after workflow demand is validated.
