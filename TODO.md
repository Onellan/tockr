# TODO

- Phase 1 Toggl-inspired features now implemented: favorites, task tracking, project estimate/budget dashboard, saved reports, and task-aware reporting filters.
- Dropdown/favicons follow-up: add row action overflow menus once rows gain more than one secondary action.
- Add PostgreSQL repository implementation when needed.
- Add optional TOTP if a deployment requires it.
- Add richer invoice templates after validating the lightweight HTML/CSV path.
- Add calendar views only if users need them.
- Add data import tests against representative Kimai exports.
- Add optional account/profile settings screen when user self-service settings are needed.
- Add optional collapsible sidebar only if the menu grows beyond the current workflow groups.
- Add explicit workspace switcher once multiple workspaces are common in production.
- Add richer project membership editing after validating the first scoped RBAC implementation.
- Add historical rates and user cost tracking before full profitability reporting.
- Add scheduled/shared reports with signed expiring tokens.
- Add approvals and locked periods only after workflow demand is validated.
